# 🎯 المشروع الختامي: بناء لوحة تحكم سياحية (Travel Dashboard)

هذا هو التحدي الختامي لقسم **"Describing the UI"**. الهدف هو دمج كل ما تعلمته في الأيام العشرة السابقة لبناء واجهة مستخدم متكاملة، منظمة وجميلة.

## 📝 وصف المهمة (Task Description)

عليك بناء صفحة واحدة (Dashboard) تعرض قائمة بالوجهات السياحية العالمية. يجب أن تحتوي الصفحة على:

1. **هيكل مكونات متداخل:** (Navbar, Sidebar, Main Content).
2. **قائمة ديناميكية:** عرض قائمة من الوجهات باستخدام `map`.
3. **تصيير شرطي:** إظهار علامة "Featured" أو "New" على بعض الوجهات بناءً على شروط.
4. **استخدام الـ Props:** تمرير البيانات بالكامل عبر الـ Props، مع استخدام مكون مغلف (Card Mirroring) باستخدام الـ `children`.
5. **مكونات نقية:** التأكد من أن جميع المكونات الحسابية (مثل حساب الخصم أو عدد الأيام) تظل نقية تماماً.

---

## 📏 القواعد الواجب اتباعها (Rules of the Road)

- **[RULE 1]**: لا تستخدم `useState` أو أي Hook لإدارة الحالة (هذا للمستوى القادم). اعتمد كلياً على تمرير الـ Props.
- **[RULE 2]**: يجب أن يكون كل مكون في ملف مستقل (Systematic Component Splitting).
- **[RULE 3]**: استخدم الـ JSX Variables لجعل الكود أنيقاً، خاصة في منطق التصيير الشرطي.
- **[RULE 4]**: الكود يجب أن يكون منظماً مع اتباع قاعدة الـ `PascalCase` للمكونات.
- **[RULE 5]**: الالتزام بتنسيقات الـ Premium Dark Theme اللي كنا ماشيين عليها.

---

## 🛠️ كيف تبدأ؟ (How to Execute)

1. استخدم مجلد المشروع هذا: `Describing the UI/FinalProject-DescribingTheUI`.
2. قم بتعريف مصفوفة البيانات (Data Array) في ملف منفصل `data.js`.
3. ابدأ ببناء المكونات الصغيرة (Leaf Components) مثل `Avatar` و `Badge` و `PriceTag`.
4. اجمعها داخل المكونات الأكبر مثل `DestinationCard`.
5. ركب الصفحة كاملة في `App.jsx`.

---

## 🏆 النتيجة النهائية المتوقعة (Final Outcome)

- واجهة مستخدم تعرض الأماكن السياحية بشكل احترافي.
- كود نظيف وسهل القراءة يتبع أفضل ممارسات ريأكت (Modern Patterns).
- هيكل شجري (Render Tree) واضح ومنطقي.

**بالتوفيق يا بطل! انطلق وأرنا إبداعك.** 🚀
